1. pull before starting
2. push after finishing


to clone 
	git clone (ssh link)

to add 
	git add (file name/path)

to commit
	git commit -m "(a comment, hopefully something that's actually useful)"
